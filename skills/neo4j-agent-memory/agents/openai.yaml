interface:
  display_name: "Neo4j Agent Memory"
  short_description: "Recall and preserve structured cross-session graph memory."
  default_prompt: "Use $neo4j-agent-memory for the requested Neo4j recall, structured memory write, or bounded thread ingestion, respecting external-write and freshness boundaries."
